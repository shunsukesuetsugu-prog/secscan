# Benchmark fixture: AWS-shaped access key with the unique marker
# "SECSCAN" embedded so the value is obviously synthetic. The key
# is regex-valid for the AWS-access-key gitleaks rule but is not on
# any documentation allowlist, so it surfaces as a finding.
# This is NEVER a real AWS credential — the body deliberately spells
# 'BENCHMARKSECSCAN' which AWS would never issue.
AWS_ACCESS_KEY_ID=AKIABENCHMARKSECSCAN
AWS_SECRET_ACCESS_KEY=BenchmarkSecscanFixtureSecretXXXXXXXXXX0
