{
  "tool_under_test": "secscan secrets",
  "comparison_tool": "gitleaks",
  "notes": [
    "Each fixture below holds a credential SHAPE (regex-matchable) but a",
    "structurally / cryptographically invalid value. See _manifest.json for",
    "the SHA-256 hash and invalidity justification for each file.",
    "Recall = |files for which scanner emitted at least one finding| / |files|.",
    "The 'clean' siblings are measured for false-positive rate."
  ],
  "expected_findings": [
    {"file": "aws_invalid.txt", "rule_id_pattern": "aws"},
    {"file": "github_pat_invalid.txt", "rule_id_pattern": "github"},
    {"file": "slack_invalid.txt", "rule_id_pattern": "slack"},
    {"file": "npm_token_invalid.txt", "rule_id_pattern": "npm"}
  ]
}
