# Benchmark fixture: GitHub PAT shape with a structurally valid prefix
# but no checksum / signature backing. GitHub rejects these immediately
# (the trailing characters do not match a real-issued token).
GITHUB_TOKEN=ghp_000000000000000000000000000000000000
