# Benchmark fixture: GitHub PAT-shaped token with "secscan-benchmark"
# embedded in the trailing body so the value is unmistakably synthetic.
# It is regex-valid for gitleaks' github-pat rule but is not a real
# token — GitHub PATs encode a checksum that this fixture deliberately
# fails (the body is alphabetic / patterned, not the random hex GitHub
# issues).
GITHUB_TOKEN=ghp_secscanbenchmarkfixturetokenAAAAAA00
