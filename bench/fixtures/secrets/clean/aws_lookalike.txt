# Borderline-clean fixture (GLM 1st review pin):
# Contains "AKIA"-prefixed strings that are NOT AWS keys — they are
# user-facing identifiers (e.g. ticket numbers, internal product
# codes). A scanner that flags these emits a false positive.
TICKET_ID=AKIA-2024-CUSTOMER-SUPPORT-001
INTERNAL_ID=AKIA-DEPT-FINANCE
# Real AWS keys are exactly 20 characters of [A-Z0-9] after "AKIA".
# These have hyphens and lowercase letters, so they should NOT match.
